on:
  push:
    branches:
      - master



jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'testing github action'
        run: pwd
      - name: 'name'
        run: echo Kareem Mohamed abd el baky Mahmoud
      - name: 'ls'
        run: ls
      - uses: actions/checkout@v3
      - name: 'ls'
        run: ls
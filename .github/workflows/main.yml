on:
  push:
    branches:
      - master



jobs:
  build:
    runs-on: windows

    steps:
      - name: 'testing github action'
        run: pwd
      - name: 'Kareem Mohamed abd el baky Mahmoud'
        run: echo name


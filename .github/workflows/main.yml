on:
  push:
    branches:
      - master



jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'testing github action'
        run: pwd
      - name: 'Kareem Mohamed abd el baky Mahmoud'
        run: echo name


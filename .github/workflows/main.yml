on:
  push:
    branches:
      - master



jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: 'testing github action'
        run: pwd
      - name: 'name'
        run: echo Kareem Mohamed abd el baky Mahmoud
      - name: 'ls'
        run: ls
      - name: 'login to docker hub'
        run: docker login --username ${{ secrets.DOCKERHUBUSERNAME }} --password ${{ secrets.DOCKERHUBPASSWORD }}
      - name: 'build docker image'
        run: docker build -t ${{ secrets.DOCKERHUBUSERNAME }}/myimg2 .
      - name: 'push to docker hub'
        run: docker push ${{ secrets.DOCKERHUBUSERNAME }}/myimg2
      #- name: 'run image'
      #  run: docker 